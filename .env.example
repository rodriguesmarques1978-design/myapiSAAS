# Supabase — usado pela API route /api/waitlist para guardar as inscricoes.
# Dashboard → Project Settings → API
SUPABASE_URL=
# Service role key: ignora RLS, so pode ser usada server-side. Nunca commitar.
SUPABASE_SERVICE_ROLE_KEY=
